{
  "name": "xliv-project",
  "version": "0.0.0",
  "description": "Base for XLIV project.",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "test": "lerna run test",
    "build": "lerna run build",
    "setup": "lerna run setup",
    "ci-publish": "lerna publish --canary --yes --skip-git"
  },
  "devDependencies": {
    "lerna": "^2.0.0-rc.1"
  }
}
