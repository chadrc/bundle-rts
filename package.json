{
  "name": "xliv-project",
  "version": "0.0.0",
  "description": "Base for XLIV project.",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "test": "lerna run test",
    "build": "lerna run build"
  },
  "devDependencies": {
    "lerna": "2.0.0-beta.38"
  }
}
